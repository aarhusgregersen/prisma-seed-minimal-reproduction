# Minimal reproduction environment
# This DATABASE_URL doesn't need to point to a real database
# as we're just testing if the seed command is triggered

DATABASE_URL="postgresql://user:password@localhost:5432/prisma_seed_repro?schema=public"
